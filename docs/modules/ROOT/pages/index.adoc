= Terminal UI Kit

*Terminal UI Kit* is a C{plus}{plus}20/23 library of high-level, production-ready
components for modern interactive terminal applications, built on top of
https://github.com/ArthurSonzogni/FTXUI[FTXUI].

It provides the components FTXUI intentionally leaves out: virtualized
lists and documents, streaming output, a multiline editor, Markdown and
syntax-highlighted code rendering, diff views, log views, completion and
command-palette UX, and terminal integrations (clipboard, images, capability
detection).

The library returns ordinary `ftxui::Component` and `ftxui::Element` objects,
does not run its own event loop, and stays independent of any specific
application domain -- see the xref:prd.adoc[full product requirements document]
for the complete specification.

== Status

Foundation stage (pre-0.1.0). Landed so far:

* repository foundation -- CMake, Xmake, CI, install/package export (PR 1);
* testing foundation -- GoogleTest/CTest wiring, a virtual-screen rendering-test helper (PR 2);
* Core Text Model -- `TextPosition`, `TextRange`, `StyledText`/`TextSpan`, UTF-8-safe word wrapping (PR 3).

No `ftxui::Component` widgets exist yet. See the xref:prd.adoc[PRD] section 57
for the milestone roadmap and section 63 for the initial pull request plan.

== Building

CMake is the primary, authoritative build system; Xmake is available as a
secondary developer-facing frontend on Linux.

=== CMake

[source,sh]
----
cmake --preset debug
cmake --build --preset debug
ctest --preset debug
----

Feature options are documented in `cmake/TerminalUiKitOptions.cmake`.
All optional dependencies and the test/example/benchmark subprojects default
to `OFF`, so a bare configure never touches the network.

=== Xmake

[source,sh]
----
xmake config --mode=debug --yes
xmake build
----

== Repository Layout

[source,text]
----
include/terminal_ui_kit/   Public headers, one directory per module
src/terminal_ui_kit/       Implementation
tests/terminal_ui_kit/     Unit, rendering, interaction, integration, fuzz tests
examples/                  Example applications (chat, diff viewer, log viewer, ...)
benchmarks/                Performance benchmarks
docs/                      This documentation site (Antora/AsciiDoc)
cmake/                     CMake helper modules
doc/                       Planning documents (PRD source, as Markdown)
----

== Documentation

* xref:prd.adoc[Product Requirements Document]
* xref:contributing.adoc[Contributing]
* xref:security.adoc[Security policy]
* xref:changelog.adoc[Changelog]

Component and architecture guides (getting started, editor, virtualization,
markdown, diff, theming, ...) will be added under this site as each module
is implemented -- see xref:prd.adoc[the PRD] section 53 for the planned set.

== License

MIT -- see the https://github.com/Artem535/Terminal-UI-Kit/blob/main/LICENSE[LICENSE] file.
