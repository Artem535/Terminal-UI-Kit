= Changelog

All notable changes to this project are documented here. The format follows
https://keepachangelog.com/en/1.1.0/[Keep a Changelog], and the project
adheres to https://semver.org/[Semantic Versioning] (see xref:prd.adoc[the PRD]
section 45 for the project's specific stability rules before/after 1.0).

== [Unreleased]

=== Added

- TranscriptView component (`components/transcript.h`, `components/transcript_model.h`)
  with a heterogeneous block list, a mutable streaming tail, follow-end,
  virtualized mixed-height rendering, search, bookmarks, and copy/details
  callbacks. Includes unit/rendering/interaction tests, a benchmark, and the
  `transcript_view_example` interactive application (#H3).
- CompletionPopup component: reusable fuzzy autocomplete popup with sync /
  async providers, stale-result cancellation, viewport-aware placement, and a
  completion_popup_example application (#M3)
- Terminal capability model (`TerminalUiKit::Terminal`): `ColorDepth` and
  `TerminalCapabilities` value types, an injectable `EnvironmentProvider`
  (`SystemEnvironment` plus a deterministic snapshot in tests), a static,
  conservative detection policy (`DetectTerminalCapabilities`), and explicit
  `CapabilityOverrides` for enabling/disabling individual capabilities.
- `terminal_capabilities` interactive example application.
- LogView component with follow/pause and severity badges (#PR7B)
- streaming_log_viewer interactive example application
- render_styled_text utility for StyledText-to-FTXUI Element conversion
- `format_line_number` shared helper and line-number gutter underflow fix in
  `CodeView` and `VirtualDocument` (numbers wider than the gutter render fully
  without truncation, underflow, or excessive padding)
- line_number_formatting_example interactive example application

* Repository foundation: CMake + Xmake build systems, vcpkg manifest,
  directory skeleton for `include/`, `src/`, `tests/`, `examples/`,
  `benchmarks/`, and `docs/`, code style configuration (clang-format,
  clang-tidy), CI workflow skeleton, and governance documents.
* Testing foundation: GoogleTest/CTest wiring and a virtual-screen
  rendering-test helper (`terminal_ui_kit::test_support::render_to_text`).
* Core Text Model: `TextPosition`, `TextRange`, `TextStyle`/`Color`,
  `StyledText`/`TextSpan`, and a UTF-8 codepoint-safe `wrap_plain_text()`
  word-wrap utility.
* Documentation site: AsciiDoc content published via Antora, replacing the
  Markdown docs that previously lived only in the repository root.
* Variable-height `VirtualList` support with deterministic height estimates,
  measured layout caching, prefix-sum range lookup, and scroll anchoring when
  visible rows change height.
* Streaming document and log model support: chunk-safe UTF-8 ingestion,
  ANSI SGR parsing into `StyledText`, retained log entries with severity and
  substring filtering, and revision tracking for incremental consumers.
  The interactive `LogView` and streaming log viewer example are planned for
  PR7B.
