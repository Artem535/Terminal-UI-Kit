-- Xmake is a secondary, developer-facing Linux frontend (PRD section 11.2);
-- CMake remains authoritative for what is actually built and tested. This
-- module registers the Toast example application for xmake builds.
includes("toast_example")